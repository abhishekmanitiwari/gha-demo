name: input-demo
on: 
  workflow_dispatch: 
    inputs:
      environment:
        description: choose the environment to deploy
        required: true
        default: dev

jobs:
  input-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Deploye the App
        run: echo "Deploye the application to ${{ inputs.environment }}"
